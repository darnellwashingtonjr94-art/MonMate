include: package:flutter_lints/3.0.2/lib/recommended.yaml

linter:
  rules:
    # Add any custom lint overrides here if needed
    avoid_print: true
    prefer_const_constructors: true

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
